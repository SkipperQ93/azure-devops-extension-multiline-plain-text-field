{
  "name": "azure-devops-extension-multiline-plain-text-field",
  "version": "1.0.0",
  "description": "This custom control allows you to add a simple multiline plain text field.",
  "main": "multilinePlainTextFieldControl.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rimraf ./dist ./out",
    "package": "npm run clean && tfx extension create --manifest-globs vss-extension.json --output-path out"
  },
  "keywords": [],
  "author": "Krystian Puchalski",
  "license": "EUPL-1.2",
  "dependencies": {
    "vss-web-extension-sdk": "^5.141.0"
  },
  "devDependencies": {
    "rimraf": "^6.0.1",
    "tfx-cli": "^0.17.0"
  }
}
